pip install keras==2.3.1 pip install tensorflow==1.14.0


AttributeError: 'str' object has no attribute 'decode'   keras load model 
pip install h5py==2.10.0



InceptionV3_model.fit_generator(
        train_generator,
        steps_per_epoch = nbr_train_samples,
        epochs = nbr_epochs,
        validation_data = validation_generator,
        validation_steps = nbr_validation_samples,
        callbacks = best_model)

